<!DOCTYPE html>
<html>
<head>
    <title><%= formTitle %> - La Lune Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5 mb-5" style="max-width: 800px;">
        <div class="card shadow border-0">
            <div class="card-header bg-white py-3">
                <h4 class="mb-0" style="color: #D4A5A5;"><%= formTitle %></h4>
            </div>
            <div class="card-body p-4">
                <form action="<%= action %>" method="POST">
                    
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="form-label fw-bold">Tên bánh (*)</label>
                            <input type="text" name="name" class="form-control" value="<%= product.name || '' %>" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label fw-bold">Giá tiền (VNĐ) (*)</label>
                            <input type="number" name="price" class="form-control" value="<%= product.price || '' %>" required>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Link Hình ảnh (URL)</label>
                        <input type="text" name="image" class="form-control" value="<%= product.image || '' %>" placeholder="Ví dụ: /images/banh.png hoặc link trên mạng">
                        <div class="form-text">Bạn có thể lấy link ảnh trên mạng dán vào đây.</div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-4">
                            <label class="form-label fw-bold">Xuất xứ</label>
                            <input type="text" name="origin" class="form-control" value="<%= product.origin || '' %>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-bold">Trọng lượng</label>
                            <input type="text" name="weight" class="form-control" value="<%= product.weight || '' %>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-bold">Ý nghĩa</label>
                            <input type="text" name="meaning" class="form-control" value="<%= product.meaning || '' %>">
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Thành phần</label>
                        <textarea name="ingredients" class="form-control" rows="2"><%= product.ingredients || '' %></textarea>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Mô tả chi tiết</label>
                        <textarea name="description" class="form-control" rows="4"><%= product.description || '' %></textarea>
                    </div>

                    <div class="d-flex justify-content-end gap-2">
                        <a href="/admin" class="btn btn-secondary">Quay lại</a>
                        <button type="submit" class="btn btn-primary px-4" style="background-color: #D4A5A5; border: none;">Lưu sản phẩm</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>