<!DOCTYPE html>
<html>
<head>
    <title>Thanh toán - La Lune Bakery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5 mb-5">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="card card-custom text-center p-4 border-0 shadow-lg">
                    <h3 style="color: var(--main-color);">Thanh Toán Chuyển Khoản</h3>
                    <p class="text-muted">Vui lòng mở App Ngân hàng để quét mã</p>
                    
                    <div class="p-3 bg-white rounded shadow-sm d-inline-block mb-3 border">
                        <% 
                            const BANK_ID = "BIDV"; 
                            const ACCOUNT_NO = "5811689911"; 
                            const TEMPLATE = "compact2"; // Giao diện QR
                            const DESCRIPTION = `Thanh toan don hang ${user.email}`; 
                            // Link tạo QR tự động của VietQR
                            const qrLink = `https://img.vietqr.io/image/${BANK_ID}-${ACCOUNT_NO}-${TEMPLATE}.png?amount=${total}&addInfo=${DESCRIPTION}&accountName=LA LUNE BAKERY`;
                        %>
                        
                        <img src="<%= qrLink %>" width="100%" alt="Mã QR Thanh Toán">
                    </div>

                    <div class="alert alert-warning text-start">
                        <small>
                            <strong>Chủ tài khoản:</strong> LA LUNE BAKERY<br>
                            <strong>Số tiền:</strong> <%= total.toLocaleString('vi-VN') %> VNĐ<br>
                            <strong>Nội dung:</strong> <%= DESCRIPTION %>
                        </small>
                    </div>

                    <a href="/" class="btn btn-outline-secondary w-100 mt-2">Tôi đã chuyển khoản xong</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>