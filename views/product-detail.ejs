<!DOCTYPE html>
<html>
<head>
    <title><%= product.name %> - La Lune Bakery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- Navbar (Copy giống trang chủ hoặc dùng lệnh include nếu biết) -->
    <nav class="navbar navbar-light bg-light sticky-top shadow-sm py-3">
    <div class="container d-flex justify-content-between align-items-center">
        
        <a href="/" class="btn-back-home">
            <i class="fas fa-chevron-left"></i> Quay lại Tiệm Bánh
        </a>

        <a href="/" style="text-decoration: none;">
            <span style="font-family: 'Times New Roman', serif; font-weight: bold; font-size: 1.5rem; color: var(--main-color);">La Lune</span>
        </a>
        
    </div>
</nav>

    <div class="container mt-5">
        <div class="row align-items-center">
            <!-- Cột trái: Ảnh lớn -->
            <div class="col-md-6 mb-4">
                <img src="<%= product.image %>" class="img-fluid rounded shadow-sm w-100" style="object-fit: cover; height: 500px;" alt="<%= product.name %>">
            </div>

            <!-- Cột phải: Thông tin chi tiết -->
            <div class="col-md-6">
                <h1 class="display-4" style="color: var(--main-color); font-family: 'Times New Roman', serif;"><%= product.name %></h1>
                <h3 class="text-danger my-3"><%= product.price.toLocaleString('vi-VN') %> VNĐ</h3>
                
                <p class="lead"><%= product.description %></p>

                <div class="card card-custom p-3 mt-4" style="background-color: #fffaf0;">
                    <ul class="list-unstyled mb-0">
                        <li class="mb-2"><strong>🌍 Nguồn gốc:</strong> <%= product.origin %></li>
                        <li class="mb-2"><strong>⚖️ Khối lượng:</strong> <%= product.weight %></li>
                        <li class="mb-2"><strong>✨ Nguyên liệu đặc biệt:</strong> <%= product.ingredients %></li>
                        <li class="mb-2"><strong>🌙 Ý nghĩa:</strong> <%= product.meaning %></li>
                    </ul>
                </div>

                <div class="mt-4">
                    <form action="/add-to-cart" method="POST">
                        <input type="hidden" name="productName" value="<%= product.name %>">
                        <input type="hidden" name="price" value="<%= product.price %>">
                        <input type="hidden" name="img" value="<%= product.image %>">
                        
                        <div class="d-flex align-items-center">
                            <!-- Nút đặt mua lớn -->
                            <button type="submit" class="btn btn-lune btn-lg px-5">Đặt Mua Ngay</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>